<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5">

    <!-- 标题栏 -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:elevation="0dp"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:navigationIconTint="@android:color/white"
            app:title="管理赛事"
            app:titleTextColor="@android:color/white" />

        <!-- Tab切换 -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:background="?attr/colorPrimary"
            android:elevation="0dp"
            app:tabIndicatorColor="@android:color/white"
            app:tabSelectedTextColor="@android:color/white"
            app:tabTextColor="#CCFFFFFF"
            app:tabMode="fixed"
            app:tabGravity="center"
            app:tabPaddingStart="24dp"
            app:tabPaddingEnd="24dp" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- 主内容区 - 使用 SwipeRefreshLayout -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingBottom="80dp">

                <!-- 进度条 -->
                <ProgressBar
                    android:id="@+id/progressMyResults"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginTop="32dp"
                    android:visibility="gone" />

                <!-- 未开始赛事板块 -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvNotStartedRaces"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:nestedScrollingEnabled="false"
                    android:visibility="gone" />

                <!-- 进行中赛事板块 -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvOngoingRaces"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:nestedScrollingEnabled="false"
                    android:visibility="gone" />

                <!-- 已结束赛事板块 -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvEndedRaces"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:nestedScrollingEnabled="false"
                    android:visibility="gone" />

                <!-- 空状态页面 -->
                <LinearLayout
                    android:id="@+id/layoutEmptyState"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:padding="48dp"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="暂无赛事"
                        android:textSize="16sp"
                        android:textColor="#999999"
                        android:gravity="center" />

                </LinearLayout>

                <!-- 未开始赛事空状态提示 -->
                <TextView
                    android:id="@+id/tvNotStartedEmpty"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:text="暂无未开始赛事"
                    android:textColor="#999999"
                    android:textSize="14sp"
                    android:visibility="gone" />

                <!-- 进行中赛事空状态提示 -->
                <TextView
                    android:id="@+id/tvOngoingEmpty"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:text="暂无进行中赛事"
                    android:textColor="#999999"
                    android:textSize="14sp"
                    android:visibility="gone" />

                <!-- 已结束赛事空状态提示 -->
                <TextView
                    android:id="@+id/tvEndedEmpty"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:text="暂无已结束赛事"
                    android:textColor="#999999"
                    android:textSize="14sp"
                    android:visibility="gone" />

            </LinearLayout>

        </androidx.core.widget.NestedScrollView>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
